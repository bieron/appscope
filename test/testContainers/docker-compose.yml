version: '3.5'
services:

  tutorial:
    build:
      context: tutorial
      # cache_from:
      #  - type=local,src=/tmp/.buildx-cache
      # cache_to:
      #  - type=local,dest=/tmp/.buildx-cache
